stages:
  - build

backend_build:
  stage: build
  script:
    - cd backend
    - mvn clean install

frontend_build:
  stage: build
  script:
    - cd frontend
    - npm ci
    - npm run test
    - npm run build
